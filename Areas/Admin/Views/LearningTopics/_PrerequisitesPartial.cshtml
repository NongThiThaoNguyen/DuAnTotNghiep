@model IEnumerable<DuAnTotNghiep.Models.DTOs.Topic.TopicOptionDto>

<ul>
    @foreach (var prereq in Model)
    {
        <li>@prereq.Name</li>
    }
</ul>
@if (!Model.Any())
{
    <p class="text-muted">No prerequisites.</p>
}
